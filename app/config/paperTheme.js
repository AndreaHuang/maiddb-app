import color from "./color";
export default {
    colors:{
        accent: color.primary,
        primary: color.primary,
        text: color.dark,
        placeholder:color.medium,  
        error:color.error  
    }
}